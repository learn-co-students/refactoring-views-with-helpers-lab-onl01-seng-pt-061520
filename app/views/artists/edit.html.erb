<h1>Editing <%= @artist.name %></h1>

<%= form_for @artist  do |f| %>
  <label><%= display_artist @artist %></label><br>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br />

  <%= f.submit %>
<% end %>
